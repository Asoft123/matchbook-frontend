import { Link } from "react-router-dom";
import styled from "styled-components";

export const AppLink = styled(Link)`
  text-decoration: none;
  cursor: pointer;
  display: inline-flex;
`;
export const AppLinkSearch = styled(Link)`
  text-decoration: none;
  cursor: pointer;
`;
export const GlobalCenter = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;
